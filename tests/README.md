# Demo Tests

This directory contains focused regression tests for the demo application.

Run the test suite with:

```bash
npm test
```

Jest runs the tests in `__tests__/`, including the `/api/answer` route test.
