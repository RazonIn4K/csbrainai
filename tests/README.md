# Demo Tests

This directory contains automated tests for the demo application.

## Running the Tests

To run the tests, use the following command:

```bash
npm test
```

This will run all tests in the `__tests__` directory, including the integration test for the `/api/answer` endpoint.
